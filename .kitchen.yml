---
driver:
  name: vagrant
  provider: libvirt
  customize:
    volume_cache: "\'writeback\'"

provisioner:
  name: chef_solo

platforms:
  - name: centos-7.2

suites:
  - name: default
    run_list:
      - recipe[dmlb2000_pipeline::master]
      - recipe[dmlb2000_pipeline::slave]
      - recipe[dmlb2000_pipeline::gerrit]
    attributes:
