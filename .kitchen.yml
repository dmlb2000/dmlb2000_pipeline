---
driver:
  name: vagrant
  provider: libvirt

provisioner:
  name: chef_solo

platforms:
  - name: ubuntu-14.10

suites:
  - name: default
    run_list:
      - recipe[dmlb2000_pipeline::default]
    attributes:
